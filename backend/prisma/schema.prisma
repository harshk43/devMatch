// Prisma schema for DevMatch
// Models (User, Skill, Swipe, Match) will be added in Step 2.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
